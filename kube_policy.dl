
import pod

output relation PodExists(name: string, status: string)

PodExists(name: string, status: string) :-
            Pod(meta, _, pstatus),
            var name = meta.name.unwrap_or_default(),
            var status = pstatus.pod_ip.unwrap_or_default().
